import { notFound } from "next/navigation";
export default function ResearchDetail({ params }: { params: { slug: string } }) { if (!params.slug) notFound(); return <section className="bg-sand-light py-24"><div className="container-page max-w-3xl"><span className="eyebrow text-forest">Research &amp; Evidence</span><h1 className="mt-5 font-display text-5xl text-ink">Research from Circle Group</h1><p className="mt-7 text-lg text-ink/80">Published research and reports will be shared here as they become available.</p></div></section>; }
