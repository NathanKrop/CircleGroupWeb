import { notFound } from "next/navigation";
export default function StoryDetail({ params }: { params: { slug: string } }) { if (!params.slug) notFound(); return <section className="bg-sand-light py-24"><div className="container-page max-w-3xl"><h1 className="font-display text-5xl text-ink">Stories from the field.</h1><p className="mt-7 text-lg text-ink/80">Our first stories are on their way. Check back soon.</p></div></section>; }
